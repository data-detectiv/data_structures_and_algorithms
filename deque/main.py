"""
Types of deque
- Input restricted deque: input is restricted at a single end but allows deletion at both ends
- Output restricted deque: output is restricted at a single end but allows insertion at both ends
"""